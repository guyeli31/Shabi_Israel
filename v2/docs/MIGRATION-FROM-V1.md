# Migration from v1 → v2

Authoritative mapping table. Every v1 source file lands in exactly one v2 destination. This file is filled in incrementally as each phase progresses; by Phase 11 it is complete and used as the cutover checklist.

> Status legend: ◯ pending — ◐ partial — ● complete

## CSS files (15 files in v1, all dissolved into layered structure in v2)

| v1 file | v2 destination | Status | Phase |
|---|---|---|---|
| `css/variables.css` | split → `src/tokens/{space,radius,shadow,motion,breakpoints,color}.css` + `src/themes/_theme.css` (semantic vars) + theme overrides for `lt-*` per-theme blocks | ● | 1 |
| `css/typography-tokens.css` | replaced by `src/tokens/typography.css` (7 sizes + 4 weights per [docs/TYPOGRAPHY.md](../../docs/TYPOGRAPHY.md) §2.2) + `src/tokens/icon.css` (4 icon sizes). v1's legacy `--fs-055..--fs-200` numeric set is deliberately not ported. | ● | 1 |
| `css/typography-overrides.css` | `src/tools/typoEditor/workspace.css` (gitignored) — v1 file is a transitional hack forcing everything to `--fs-085`; v2 starts clean with the proper 7-token scale | ◯ | 7 |
| `css/themes.css` | split → `src/themes/{dark,beige,nature,vegas,casino,rainbow,x22}.css`. Theme-specific component decorations (Vegas roulette, Rainbow border stripes, X22 triangle header) deferred to the components that need them | ◐ | 1, 3 |
| `css/theme.css` | data-emphasis classes (`.val-best`, `.val-worst`, `.val-better`) — defer to component CSS (Phase 3) since they're component-level emphasis, not theme contract | ◯ | 3 |
| `css/layout.css` | universal/box-sizing reset + `html`/`body` defaults + `strong`/`b` reset + form-control inheritance → `src/base/{reset,root,elements,focus,a11y}.css`. Splash overlay extracted to `src/components/Splash/splash.css` in Phase 3. Page-level rules (`.page-container`, `.page-header`) still pending → per-page CSS in Phase 6 | ◐ | 1, 3, 6 |
| `css/components.css` | dissolved → `src/primitives/*` and `src/components/*` (PlayerCell, StatusChip, MedalRow, RankBadge, ScoreCell, ChartTooltip — Phase 3) | ◐ | 2, 3 |
| `css/navigation.css` | `src/components/Navigation/navigation.css` + `src/components/Breadcrumbs/breadcrumbs.css` + `src/components/SearchBox/searchBox.css` | ● | 3 |
| `css/dashboard.css` | `src/pages/dashboard/dashboard.css` + extractions | ◯ | 3, 6 |
| `css/league-header.css` | `src/components/LeagueHero/leagueHero.css` | ● | 3 |
| `css/player-general.css` | `src/components/PlayerHero/playerHero.css` (V7 + V12) + `src/pages/playerGeneral/playerGeneral.css` (page-only chrome) | ◐ | 3, 6 |
| `css/index-dashboard.css` | `src/pages/landing/landing.css` | ◯ | 6 |
| `css/sections.css` (NEW — single source for section frame/heading/collapse; see 2026-06-17 bug-fix row) | `src/components/Section/section.css` (the `Section` component/primitive — `.app-section` / `--card` / `.app-section-h2` + the one collapse mechanism). Consumed by landing/dashboard/player/admin pages. | ◯ | 3 |
| `css/tabs.css` (segmented tab bar — pairs with appTabs.js) | `src/primitives/SegmentedTabs/segmentedTabs.css` (see 2026-06-09 row) | ◯ | 2 |
| `css/subtabs.css` (NEW — single source for in-section sub-tabs: `.subtab--pill` + `.subtab--accordion`; see 2026-06-18 row) | `src/components/SubTabs/subTabs.css` (pill + accordion variants). Replaces the former per-page `.pg-tab`/`.achv-tab` (duplicated) and the `.rem-tab-*` block duplicated across dashboard.css + admin.css. | ◯ | 3 |
| `css/admin.css` | split → FF format chrome (`.ff-wrap`, `.admin-table.font-large` block, scroll-shadow) → `src/tables/FormTable/formTable.css` (Phase 5); admin-page chrome → `src/pages/admin/admin.css` (Phase 8); form controls → `src/primitives/FormField/` (Phase 2); F3↔FF reconciliation (suppress hairline, match-block state colors, round-card max-height) → `src/pages/admin/roundEditor.css` (Phase 8) | ◐ | 2, 5, 8 |
| `css/admin-button.css` | `src/components/AdminButton/adminButton.css` | ● | 3 |
| `css/theme-picker.css` | `src/components/ThemePicker/themePicker.css` | ● | 3 |
| `css/typoEditor.css` | `src/tools/typoEditor/typoEditor.css` | ◯ | 7 |

## JS files

### Data layer (`js/data/`)

| v1 file | v2 destination | Status | Phase |
|---|---|---|---|
| `csvParser.js` | `src/data/csvParser.js` | ● | 4 |
| `leagueLoader.js` | `src/data/leagueLoader.js` (paths via `/data/` alias) | ● | 4 |
| `playersMetadata.js` | `src/data/playersMetadata.js` (path uses `/data/` alias) | ● | 4 |
| `titleConstants.js` (with TIER_COLORS) | **split** → `src/data/titleConstants.js` (data only) + `src/data/titleStyleMap.js` (CSS class map). Tier colour classes are now owned by Phase 3 components (PlayerCell title badges, PlayerHero chips/ribbons). | ● | 3, 4 |
| `compute/matchHistory.js` (v1 lived under compute/, but is mostly I/O + a merge — moved to data/ in v2) | `src/data/matchHistory.js` | ● | 4 |

### Compute layer (`js/compute/`)

| v1 file | v2 destination | Status | Phase |
|---|---|---|---|
| `stats.js` | `src/compute/stats.js` | ● | 4 |
| `rankings.js` | `src/compute/rankings.js` | ● | 4 |
| `colorScale.js` (with hex constants) | `src/components/ColorScale/colorScale.js` — theme-aware red→amber→green interpolation. Compute moved into the ColorScale component so ScoreCell can import it directly without going through `compute/`. | ● | 3 |
| `leagueTypes.js` | `src/compute/leagueTypes.js` | ● | 4 |
| `matchHistory.js` | moved to `src/data/matchHistory.js` (see Data layer) | ● | 4 |

### Render layer (`js/render/`)

| v1 file | v2 destination | Status | Phase |
|---|---|---|---|
| `landingPage.js` | `src/pages/landing/landing.js` | ◯ | 6 |
| `leaguePage.js` | `src/pages/league/league.js` | ◯ | 6 |
| `dashboardPage.js` | `src/pages/dashboard/dashboard.js` + `src/components/ExportTableImage/exportTableImage.js` (replaces B6a/b/c + leaguePage export funcs with one reusable `exportToImage()`) | ◐ | 3, 6 |
| `playerPage.js` | `src/pages/player/player.js` | ◯ | 6 |
| `playerGeneralPage.js` | `src/pages/playerGeneral/playerGeneral.js` | ◯ | 6 |
| `navigation.js` | `src/components/Navigation/navigation.js` (+ Breadcrumbs + SearchBox components) | ● | 3 |
| `render/appTabs.js` (NEW — `mountAppTabs`) | `src/primitives/SegmentedTabs/segmentedTabs.js` (see 2026-06-09 row) | ◯ | 2 |
| `render/sectionCollapse.js` (NEW — `wireSectionCollapse`) | folds into the `Section` component's behaviour in `src/components/Section/section.js` (see 2026-06-17 row) | ◯ | 3 |
| `render/subTabs.js` (NEW — `mountPillTabs` + `mountAccordionTabs`) | `src/components/SubTabs/subTabs.js` (see 2026-06-18 row) | ◯ | 3 |
| `themePicker.js` | `src/components/ThemePicker/themePicker.js` | ● | 3 |
| `adminButton.js` | `src/components/AdminButton/adminButton.js` (login modal flow stays in admin page — Phase 8) | ◐ | 3, 8 |
| `splash.js` | `src/components/Splash/splash.js` | ● | 3 |
| `leagueHeader.js` | `src/components/LeagueHero/leagueHero.js` (V13 + V16) | ● | 3 |
| `playerHeader.js` | `src/components/PlayerHero/playerHero.js` (V7 + V12, watermark logic preserved) | ● | 3 |
| `playerBarChart.js` | `src/components/PlayerBarChart/playerBarChart.js` (now uses ChartTooltip component) | ● | 3 |
| `stickyShadow.js` | `src/tables/MFTable/stickyShadow.js` (internal to table system) | ◯ | 5 |
| `typoEditor.js` | `src/tools/typoEditor/typoEditor.js` (full refactor — 7+4+4 spec) | ◯ | 7 |

### Utils (`js/utils/`)

| v1 file | v2 destination | Status | Phase |
|---|---|---|---|
| `helpers.js` | split → `src/utils/{urlParams,formatting,flagUrl}.js`. `appendExportCredit()` already absorbed by `components/ExportTableImage` in Phase 3, so it does not appear in utils. | ● | 4 |

### Admin (`js/admin/`)

| v1 file | v2 destination | Status | Phase |
|---|---|---|---|
| `playerManager.js` | `src/pages/admin/playerManager.js` | ◯ | 8 |
| `roundEditor.js` (F3 — uses FF chrome via `<div class="ff-wrap">` + `class="admin-table font-large">` in v1; rewires to `FormTable` mount fn in v2) | `src/pages/admin/roundEditor.js` | ◯ | 8 |
| `leagueManager.js` (F1 Leagues + F2 Players + F2b Add-League players + F6 Medals & Prizes — all use FF chrome in v1; F2/F2b via `ffPlayersTableHTML`, F6 via `ffMedalsTableHTML`, shared between Edit + Add League) | `src/pages/admin/leagueManager.js` | ◯ | 8 |
| `overridesList.js` (F4 View Overrides — uses FF chrome in v1) | `src/pages/admin/overridesList.js` | ◯ | 8 |
| `excelImporter.js` (now also renders the **F5** MF import-preview + the pre-stage compatibility report — see `csvValidation.js` and the Bug-fixes row below) | `src/pages/admin/excelImporter.js` | ◯ | 8 |
| `csvValidation.js` (NEW — pre-stage CSV import compatibility report + `newMatches` filter feeding F5) | `src/pages/admin/csvValidation.js` | ◯ | 8 |

### Table-lab (`table-lab/`)

| v1 path | v2 destination | Status | Phase |
|---|---|---|---|
| `formats/base/base.css` | `src/tables/_table.css` | ◯ | 5 |
| `formats/mf/mf.css` + `mf.js` | `src/tables/MFTable/{mfTable.css,mfTable.js}` | ◯ | 5 |
| `formats/sf/sf.css` + `mount.js` | `src/tables/SFTable/{sfTable.css,sfTable.js}` | ◯ | 5 |
| `formats/exp/exp.css` + `mount.js` | `src/tables/ExpandableTable/{expandableTable.css,expandableTable.js}` | ◯ | 5 |
| `formats/ff/ff.css` + `mount.js` (untracked in v1) | `src/tables/FormTable/{formTable.css,formTable.js}` — unified FF (3 cell modes per ColDef: Display/Action/Edit). See `docs/TABLE-DESIGN.md` §FF | ◯ | 5 |
| `mount-mf-table.js` | `src/tables/MFTable/mfTable.js` (`mount(el, args)`) | ◯ | 5 |
| `lab.css` + `index.html` + `lab-loader.js` | `src/tools/tableLab/{tableLab.css,tableLab.html,tableLab.js}` | ◯ | 5 |
| `presets/*.js` (presets — A1-A6, **A7**, B1-B6c, C0-C4, D, E, F1-F4, **F5**, **F6**) | `src/tables/presets/{TableCode}_{name}.js` (rename per convention). **F5 (CSV Import Preview)** is MF, not FF — read-only `mountMFTable` preset (`fontClass:'font-small'`, `stickyCols:1`); built by `buildF5` in `lab-loader.js`. **F6 (Medals & Prizes)** is FF (Display medal cell + 2 Edit cells Count/Prize); the FormTable preset reads the Count/Prize inputs via Edit-col `getValue`. FF chrome is font-large-only in the canon → F6 is font-large (a font-small FF variant would need a `.admin-table.font-small` chrome block). **A7 (Players directory)** is SF — two stacked instances (Notable Figures + Rest of Players) sharing the same cols (`Player / Status / Last Active / Title`), `stickyCols: 1`, fixed sort: status (active first) → alpha; second instance gets `showTopN: 15`. Already wired through `mountSFTable` in v1 production (the first SF call site on `index.html`) — landed 2026-06-08. | ◯ | 5 |

## HTML pages

| v1 file | v2 destination | Status | Phase |
|---|---|---|---|
| `index.html` (redirect to design-lab) | `src/pages/landing/landing.html` (no redirect; real landing) | ◯ | 6 |
| `league.html` | `src/pages/league/league.html` | ◯ | 6 |
| `dashboard.html` | `src/pages/dashboard/dashboard.html` | ◯ | 6 |
| `player.html` | `src/pages/player/player.html` | ◯ | 6 |
| `player_general.html` | `src/pages/playerGeneral/playerGeneral.html` | ◯ | 6 |
| `admin.html` | `src/pages/admin/admin.html` | ◯ | 8 |
| `design-lab.html` (chrome + iframe) | `src/tools/designLab/designLab.html` | ◯ | 7 |
| `typo-editor.html` | `src/tools/typoEditor/typoEditor.html` | ◯ | 7 |
| `design-catalogue.html` | `src/tools/designCatalogue/catalogue.html` | ◯ | 1+ (grows) |
| `table-lab/index.html` | `src/tools/tableLab/tableLab.html` | ◯ | 5 |

## Docs

| v1 file | v2 destination | Status | Phase |
|---|---|---|---|
| `docs/TYPOGRAPHY.md` | `docs/TYPOGRAPHY.md` (updated for icon tokens + new editor spec) | ◯ | 1 |
| `docs/TYPOGRAPHY-INVENTORY.md` | `docs/TYPOGRAPHY-INVENTORY.md` (populated by typoEditor) | ◯ | 7+ |
| `docs/TABLE-DESIGN.md` | `docs/TABLE-DESIGN.md` (revised per variant READMEs) | ◯ | 5 |
| `docs/plans/*` | left in `_archive_v1/docs/` (historical) | ◯ | 12 |
| `docs/audit-*` | left in `_archive_v1/docs/` (historical) | ◯ | 12 |

## Data (untouched by rebuild)

| Path | Role | Action |
|---|---|---|
| `leagues/` (repo root) | Shared by v1 and v2 via Vite alias. No change. | Keep as-is |
| `leagues/{league}/leaguedata.csv` | Per-league match data | Keep |
| `leagues/{league}/league_params.json` | Per-league config (incl. CustomFlags admin overrides) | Keep |
| `leagues/landing_settings.json` | Display order + landing logo/title | Keep |
| `leagues/players_metadata.json` (if exists) | Cross-league player metadata | Keep |

## Plan corrections discovered during the rebuild

Things [PLAN.md](PLAN.md) got wrong against the real v1 state, noted here so later phases can act on the correct list.

| What PLAN.md said | Reality (v1) | Action |
|---|---|---|
| 8 themes: `light, dark, nature, vegas, ocean, sunset, forest, monochrome` | 8 themes: `default(=light), dark, beige, nature, vegas, casino, rainbow, x22` (see `js/render/themePicker.js`) | Phase 1 ports the real list. PLAN §6.1 sentinel sweep and §10 exit criteria must use this list, not the fictional one. |
| §1 tokens include `typography.css` with 7 sizes + 4 weights | [docs/TYPOGRAPHY.md](../../docs/TYPOGRAPHY.md) §3 specifies **3** weights in production (`regular/subheading/heading` = 400/600/700); §5 allows a 4th (medium = 500) **only inside the typoEditor for exploration** | `src/tokens/typography.css` exposes all 4 weight tokens (regular/medium/subheading/heading) so the editor can experiment. Production code MUST use 3. Stylelint can later add a project-source-only rule that forbids `--fw-medium` outside `src/tools/typoEditor/`. |

## Phase 2 — primitive coverage of v1 styles

Each primitive subsumes a slice of `css/components.css` / `css/admin.css`. Components in Phase 3 will compose these primitives, fully replacing the v1 rules.

| v1 class | v2 primitive | Notes |
|---|---|---|
| `.flag`, `.flag-title` | `Flag` (sizes `md` / `lg`) | Em-based heights replace v1's fixed `16px` / `24px`. |
| `.medal`, `.medal-gold`/`silver`/`bronze` | `Badge.--circle` + `--gold/silver/bronze` | Same colour tokens. |
| `.status-pill`, `.status-running`, `.status-completed` | `Pill.--running/completed` | |
| `.league-type-pill`, `.type-doubling/regular/ubc` | `Pill.--doubling/regular/ubc` + `.--uppercase` | Uppercase opt-in (v1 was always uppercase). |
| `.retired-badge` | `Pill` (composition decision — see Phase 3) | |
| `.img-export-btn`, `.admin-button-toggle`, `.matchup-show-all-btn` | `Button.--primary` / `Button.--icon` / `Button.--pill` | |
| `.floating-btn-tooltip` (admin/theme picker hover labels) | `Tooltip` | |
| `.matchup-search-input` + various admin inputs | `FormField` (input, select, textarea variants) | |
| `td.player-cell a`, `.landing-table a`, `.matchup-table a` | `Link.--quiet` | Same "underline on hover only" behaviour. **Link-cell weight contract (2026-06-18):** every table link cell must be 600. In v1 the weight came from the cell (`td.player-cell { font-weight: 600 }`); in v2 `.link--quiet` itself now pins `font-weight: var(--fw-subheading)` so a quiet table link can never inherit body weight even outside a `player-cell`. Both belt and braces. |
| `td.league-cell a` (C1/C2 League column) | `Link.--strong` | The clickable League column on the player-general tables must read like the OPPONENT cell (semibold + quiet link + accent hover). v1 achieves this by sharing the `td.player-cell`/`td.league-cell` rule group; v2's `Link.--strong` already bundles `color: text` + `--fw-subheading` + hover-accent, so the league `<a>` uses `--strong`. As of 2026-06-18 `--quiet` also carries `--fw-subheading`, so either variant is now safe for a league link — neither renders at regular weight. See the §"Link-cell weight contract (ALL formats)" in `docs/TABLE-DESIGN.md`. |
| `.admin-welcome-avatar` | `Avatar` (initials variant, size `md`) | Replaces the bespoke gradient circle. |

## Phase 3 — component coverage of v1 styles

Each component dissolves a slice of v1's CSS and/or render-layer JS.
Pages in Phase 6 will compose these components, replacing the v1 page
renderers wholesale.

| v1 selector / function                                  | v2 component               | Notes |
|---------------------------------------------------------|----------------------------|-------|
| `td.player-cell` + inline `.flag` + `.title-abbr[-champ]` | `PlayerCell`              | Flag primitive + name `<a>` + title-abbr badges (BMAB outlined / Champ gradient) |
| `.pg-v12-statuschip`, lh status pill with dot           | `StatusChip`               | Status pill with glowing `currentColor` dot |
| `.league-type-pill.type-*`                              | `TypePill`                 | Wrapper over `Pill --doubling/regular/ubc --uppercase` |
| `tr.rank-{gold,silver,bronze}`                          | `MedalRow`                 | `classNameForRank(rank)` returns the row className |
| `.medal-{gold,silver,bronze}`                           | `RankBadge`                | Wraps `Badge --circle --gold/silver/bronze`; plain numeric for rank > 3 |
| `td` + inline RGB from colorScale + `.color-scaled`     | `ScoreCell`                | `inverted` flag flips the gradient direction |
| NEW                                                     | `FilterPill`               | Toggleable Chip; aria-pressed + Enter/Space |
| `.chart-info-panel` + `.cip-{title,row,item,k,v}`       | `ChartTooltip`             | `setItems(title, items)` API |
| `.breadcrumbs ol/li`                                    | `Breadcrumbs`              | Chevron-segment trail; current crumb auto-marked |
| `.nav-search input + .nav-search-results`               | `SearchBox`                | Standalone — `onQuery` + `setResults` |
| `.site-nav` + skip-link + leagues dropdown              | `Navigation`               | Composes SearchBox |
| `.theme-picker` floating bottom-right                   | `ThemePicker`              | 8 themes; persists to `localStorage["shabi-theme"]` |
| `.admin-button` floating bottom-left                    | `AdminButton`              | Login modal moves to Phase 8 admin page |
| `.img-export-btn`                                       | `ExportButton`             | Wraps Button + loading state |
| `.lh13-card` / `.lh16-hero`                             | `LeagueHero`               | `variant: "v13" \| "v16"` |
| `.pg-v7-card` / `.pg-v12-hero` + watermark layers       | `PlayerHero`               | `variant: "v7" \| "v12"` |
| `js/render/playerBarChart.js` + `.chart-panel/.chart-host`| `PlayerBarChart`         | Now embeds ChartTooltip; theme-aware redraw |
| `js/compute/colorScale.js` + `.color-scaled` filter     | `ColorScale`               | Math (`colorForValue`/`Inverted`/`Games`/`Level`) + visual demo strip |
| `js/utils/splash.js` + `.logo-splash`                   | `Splash`                   | 500 ms show-delay; cancelled if load finishes first |
| Three near-duplicate exporters across pages             | `ExportTableImage`         | One `exportToImage()` function; html2canvas dynamically imported |

## Dependencies added in Phase 3

| Package         | Version  | Used by              | Loading strategy |
|-----------------|----------|----------------------|------------------|
| `html2canvas`   | `^1.x`   | `ExportTableImage`   | Dynamic `import()` so pages that never export pay no bundle cost |

## Bug fixes during rebuild

Bug fixes applied to v1 during the rebuild MUST be re-applied to the corresponding v2 destination. Track them here:

| Date | v1 commit | v1 file | v2 status | Notes |
|---|---|---|---|---|
| 2026-06-18 | (staged) | **Link-cell weight unified across ALL tables (the 3 remaining drifters → 600).** Files: `css/player-general.css` (`.pg-rank-table .player-name-link` — C0 exp tables), `css/components.css` (`table.matchup-table a` — C3 Matchup League link), `css/index-dashboard.css` (`.completed-leagues-table td a` — A1 League-title link), `docs/TABLE-DESIGN.md` (the §"Clickable link cells in C1/C2" generalised into a §"Link-cell weight contract (ALL formats)" with a master list of every link cell + its weight source + a no-regress history note). **Why:** the 2026-06-12 change unified C1/C2 only; an audit of **every** documented table found three link cells still inheriting body weight (≈400) while every sibling link was 600 — **C0** player link (`.pg-rank-table .player-name-link` had colour but no weight; the lab canon `exp.css` was already 600, production had drifted), **C3** League link (`.matchup-table a` + `matchup-league-cell`, neither set weight), and **A1** League title (`<a>` with `tdClass:null` → `.completed-leagues-table td a`, no weight). Each got an explicit `font-weight: 600`, so now **100% of link cells across MF/SF/exp/FF and all page tables render at 600** — they look and behave identically. No JS/markup change; pure CSS + spec. | ◯ pending in v2 (Link primitive already enforces it — see below) | **v2 enforces the contract at the primitive level:** `src/primitives/Link/link.css` — **both** table-link variants now pin `font-weight: var(--fw-subheading)` (`.link--strong` already did; **`.link--quiet` now does too**, closing the same gap v1 had where a quiet link inherited body weight). `.player-cell` also pins it on the cell. So in v2 any table link cell — whether it uses `--quiet`, `--strong`, or sits inside `PlayerCell`/a `league-cell` equivalent — is 600 by construction; none can inherit body weight. When the lab presets (Phase 5) + pages (Phase 6) are wired, every link cell MUST resolve to `--fw-subheading`; this supersedes the 2026-06-12 row's per-table guidance with a single global rule. |
| 2026-06-18 | (staged) | **In-section sub-tabs unified into one primitive (pill + accordion).** NEW `css/subtabs.css` + `js/render/subTabs.js` exporting `mountPillTabs(mountEl, { tabs, defaultId?, pillClassFor?, onSelect })` and `mountAccordionTabs(barEl, { tabs, defaultOpenId?, onOpen })`. Migrated every sub-tab in the app and **deleted the scattered copies**: **pill** — player_general `.pg-tab` (PR Stats / Achievements / Match Records — 3 copy-pasted toggle blocks) + landing `.achv-tab` (Achievements, PR Leaders, Match Records, League Records, **and** the per-year Leaderboard type switcher — 5 sites); **accordion** — dashboard `.rem-tab-*` (Remaining Matches B6a/b/c) + admin Match Results (Round Editor / Upload CSV / View Overrides), which were a literal CSS duplicate (admin.css carried a "Keep visually in sync with css/dashboard.css" copy). CSS removed from `css/{player-general,index-dashboard,dashboard,admin}.css`; the panels now use the shared `.subtab-panel`. The pill still composes the existing `.league-type-pill .type-*` atom via `pillClassFor`; content strategy stays with each caller's `onSelect` (player re-renders a body — safe because `wireRankToggles` delegates; landing toggles prebuilt `.achv-panel`s). Accordion keeps each caller's `onOpen` (dashboard lazy-builds once; admin re-renders Upload/Overrides every open and opens Round Editor by default). **Verified** via MCP on player/landing/dashboard/admin: pills switch + active state; accordion one-open-at-a-time + lazy build; no console regressions. | ◯ pending in v2 Phase 3 | **Build a single `SubTabs` component** (`src/components/SubTabs/`) with `pill` and `accordion` variants — props `{ tabs, variant, defaultId/defaultOpenId, onSelect/onOpen }`. Pill composes the `TypePill` atom; accordion owns the `.subtab-panel` card. Every page composes it; **no** page-local sub-tab CSS or toggle JS. This is the sub-tab analog of the `Section` (2026-06-17) and `SegmentedTabs` (2026-06-09) primitives — together they mean all three tab/section families are defined once. |
| 2026-06-17 | (staged) | **`--color-on-accent` token — readable text on the accent fill in light-accent themes.** Files: `css/variables.css` (token + comment), `css/themes.css` (vegas/casino/x22 overrides), `css/navigation.css` (`.skip-link`, `.breadcrumbs .current`, `.also-plays-links a:hover`), `css/dashboard.css` (`.open-full-btn`), `css/admin.css` (`.skip-link` mirror), `css/admin-button.css` (`.admin-login-modal .btn`). **Why:** these elements hardcoded `color:#fff` on a `var(--color-accent)` background. That reads fine on the blue/green/brown/pink accents, but Vegas (`#e8c252`) + Casino (`#ffd700`) gold and X22 (`#ff9800`) orange are *light* accents → white text washed out (e.g. the active breadcrumb chevron — on **every** page, whatever its label — and the always-visible Open-full-table button). Added `--color-on-accent: #fff` to `:root` and overrode it to `var(--color-accent-light)` (the designed dark companion) in the three light-accent themes; all six call-sites now use `color: var(--color-on-accent)`. **Verified** via MCP on league/player/dashboard in vegas: white-on-gold confirmed before; token resolves to dark after. | ◯ pending (token in `_theme.css`; consumers in Phase 3) | **Token ported:** `--color-on-accent` added to `src/themes/_theme.css` `:root` (`#fff`) + overridden to `var(--color-accent-light)` in `vegas.css`/`casino.css`/`x22.css`. **Consumers updated now:** `src/components/Breadcrumbs/breadcrumbs.css` (`.breadcrumbs__crumb--current`) + `src/components/Navigation/navigation.css` (`.skip-link`) already re-pointed to it. **Still to map when built:** the dashboard `open-full` button (Phase 6) and any future accent-filled CTA must use `--color-on-accent`, never literal `#fff`. The planned `Button` primitive's accent/solid variant should default its text to `--color-on-accent`. |
| 2026-06-17 | (staged) | **Sections unified into a single shared definition (frame + heading + collapse + spacing).** NEW `css/sections.css` + `js/render/sectionCollapse.js` (`wireSectionCollapse(section, { defaultOpen, infoBtn })`). Every content section across the app is now `.app-section` (+ `.app-section--card` for the surface card) with an `.app-section-h2` heading; collapsible sections toggle `.is-collapsed` (CSS hides `> *:not(.app-section-h2)`; chevron tracks `aria-expanded`). Migrated all four surfaces onto it and **deleted the per-surface duplicates**: dashboard (`css/dashboard.css` `.dash-section`/framing/`.dash-collapse-h2`), player_general (`css/player-general.css` `.pg-section`/framing/`.pg-collapse-h2`), landing (`css/index-dashboard.css` `.lp-tab-panel > …` framing + `.collapsible-*` + leaderboard header overrides), and **admin** (`css/admin.css` duplicated `.dash-section`/`.collapsible-*` removed; admin New/Edit League sections are now framed cards like everywhere else, per the user decision). JS: `js/render/{landingPage,dashboardPage,playerGeneralPage}.js` + `js/admin/leagueManager.js` now emit `.app-section`/`.app-section-h2` and call `wireSectionCollapse`. Legacy content classes (`.dash-section`, `.leaderboard-section`, `.lp-players-section`, `.pg-section`, `.achievements-section`, …) remain on the elements **only** as hooks for per-section *content* CSS. `css/sections.css` is loaded by index/dashboard/player_general/admin. **Spacing:** inter-section gap is owned solely by `.app-section { margin-bottom: var(--space-md) }` — the `:last-child` reset was removed so the gap is identical whether a section is a direct child or wrapped in a legacy content div (verified uniform 16px on all four surfaces); the leftover `.leaderboard-section { margin-top: space-xl }` and `.achievements-section,.pr-leaders-section { margin-top: space-lg }` overrides were deleted. **Collapse defaults preserved:** dashboard Prizes closed; Predictor/What-If open; landing Completed open iff a current-year league exists; leaderboard open for current year, collapsed for past years; admin all open; player Match History open. The H2H `.matchup-header` and the leaderboard `.leaderboard-header-row` (export controls) were converted to the standard `.app-section-h2` heading. **Verified** via MCP across landing/dashboard/player/admin: every section framed, headings uniform, collapse + chevron + `aria-expanded` working, no console regressions. | ◯ pending in v2 Phase 3 | **Build a single `Section` component** (`src/components/Section/`): props `{ title?, card=true, collapsible=false, defaultOpen=true }`, slot for body, optional header `infoBtn` slot. It owns the surface-card frame, the standard `.app-section-h2`, the one collapse mechanism, and the **one inter-section spacing token** (`margin-bottom: var(--space-md)`, no `:last-child` reset). Every page (landing/league/dashboard/player/admin) composes it; there must be **no** page-local section frame/heading/collapse/spacing CSS — only per-section content rules. This supersedes the scattered `.dash-section`/`.pg-section`/`.leaderboard-section` framing the file-map rows still mention. Acceptance: section spacing is one value everywhere and independent of nesting depth. |
| 2026-06-17 | (staged) | **Admin buttons + file-inputs unified into one source; Players & Data section redesigned.** Files: `css/admin.css`, NEW `js/admin/render/formControls.js`, `js/admin/leagueManager.js`, `js/admin/playerManager.js`. **(1) One button definition:** the base `.btn` is now the single source of truth — colour is the **only** thing the variant classes (`.btn-primary/-secondary/-success/-danger`, plus `.btn-tech` = bold) change, and size is a **linear ramp** where the font comes from the type-scale tokens and the padding is in `em` so it scales proportionally at every viewport: `.btn` (md) `--fs-090` / `0.5em 1em`, `.btn-sm` `--fs-082` / `0.4em 0.8em`, `.btn-xs` `--fs-075` / `0.3em 0.65em`, `.btn-lg` `--fs-110` / `0.6em 1.25em`. The base gained a neutral surface default so variant-less buttons (the round-editor TA/TB/TD/NP `.btn-tech` toggles, which carry no colour variant) still read as buttons; filled variants set `border-color:transparent`. **Removed the two duplicate/competing definitions:** the standalone `.btn-xs` block (its own px padding + surface bg + border) and the standalone `.btn-tech` are folded into the ramp above; the `.ff-wrap .btn*` table-density overrides are kept (they're already em-linear, scaling buttons to the cell font). **(2) `.file-btn` killed** — the parallel file-picker button style (`.custom-file-input .file-btn`, its own `--fs-085` + padding) is gone; the visible "Choose File" label now reuses `.btn .btn-secondary .btn-sm`. **(3) One file picker** — NEW `filePickerHTML(id, {label, accept})` is the single definition for the hidden-input + label-button + `.file-name` span (carries `data-for` so existing change-handlers are untouched); it replaced the three copy-pasted inline blocks (Upload CSV, Upload Custom Flag, new-player flag upload). **(4) New shared layout helpers** in `admin.css`: `.input-action-row` (field flexes, button stays centred beside it — replaces the `align-items:flex-end` + `margin-top:1.4rem` hack that left the Add button floating beside the helper text), `.form-hint` (helper text on its own line under a field), `.admin-subhead` (one step above the field label on the scale, so hierarchy stays linear h2 ▸ subhead ▸ label), `.input-code` (fixed-width uppercase code input). **(5) Strip inline overrides** — removed `margin-top:1.4rem` (Add btn) and `padding:2px 8px` (the two champ-remove ✕ → now `.btn-xs`). **(6) Players & Data redesigned** (Add New League + Edit League, which share `uploadFlagPanelHTML`): a `Players` sub-block (Add-a-player input+button on one row + hint, then "Or import a roster" file row + hint, then the FF roster table — same FF variant as the Medals table above), and a `Custom Flag` sub-block (code + file + Upload on one row). Dropped a dead duplicate `#add-msg`. **Verified** via MCP on admin Add-League: Add button vertically centred with the input (`inputMid−btnMid` 0px, was −51), all file pickers identical, tech/save buttons in the round editor render correctly (surface vs primary), no console errors. | ◯ pending in v2 (Phase 8 admin + Phase 2 primitives) | **v1 kept its `.btn-*` names** (already a single shared definition; a rename to v2's `--` BEM would churn 56 call-sites + JS classList toggles for zero benefit since v1 is archived at cutover). **v1→v2 class map:** `.btn`→`.btn`; `.btn-primary/-secondary/-danger`→`.btn--primary/--secondary/--danger`; `.btn-success`→**no v2 equivalent yet** (v2 Button has primary/secondary/ghost/danger — add a `--success` variant or map success→primary); `.btn-tech` (bold, variant-less)→`.btn--ghost` or a bold modifier; `.btn-sm/-lg`→`.btn--sm/--lg`, base→`.btn--md`; `.btn-xs`→`.btn--sm` (v2 has no xs — decide if the table-density tier needs an `--xs`); `.btn-block`→`.btn--block`. The em-padding + token-font ramp **is** v2's model (`primitives/Button/button.css` already does `--fs-{micro,small,large}` + em padding) — port intact. **Gaps to fill in v2:** (a) **no FileInput primitive exists** — add `src/primitives/FileInput/` whose trigger is the Button primitive (secondary, sm) + a filename slot; `filePickerHTML` is its v1 shape. (b) `.input-action-row` + `.form-hint` + `.input-code` → fold into `FormField` (it already has `.field__hint`; add an inline action-row layout). (c) `.admin-subhead` → a section sub-heading using `--fs-large`/`--fw-subheading`, likely part of the `Section` component or admin page CSS (Phase 8). |
| 2026-06-17 | (staged) | **Unified hover tooltips + single-source popover surface.** Files: NEW `js/render/tooltip.js`, `css/components.css` (`.app-tooltip` + `.chart-info-panel`), `css/dashboard.css` (`.player-context-menu` + `.predictor-info-popup` + `.whatif-info-popup`), `css/variables.css` (popover tokens), `js/render/navigation.js` (init), `js/render/playerHeader.js` + `css/player-general.css` (V7 status dot), `js/render/adminButton.js`. **(1) One global hover tooltip:** native `title=""` tooltips are OS-rendered and unstylable, so hover hints were inconsistent (dark CSS `::after` on status dots, OS tooltips on player names in D/E, an inverted-theme span on floating buttons). `tooltip.js` installs a single document-level interceptor that catches **any** `[title]` (or `[data-tooltip]`) on hover/focus, moves the `title` to `data-tooltip` (so the OS tooltip stops firing) and shows one themed `position:fixed` popup `.app-tooltip` styled like `.player-context-menu`. `position:fixed` means it is never clipped by table-cell overflow (the reason a pure CSS `::after` was wrong for D/E). Accessibility: copies the consumed title to `aria-label` for icon-only controls. Initialised once from `initNavBar()` → covers all 5 public pages with zero call-site changes; auto-covers nav arrows, buttons, badges, ribbons, flags, the V12 status chip, etc. The V7 status dot (`data-tip`+`::after`, hardcoded dark) and the admin floating button (`.floating-btn-tooltip` span) were converted to plain `title` so they join the one mechanism; their bespoke CSS was removed. **(2) Single-source popover surface:** the elevated "text box" surfaces each re-declared bg/border/radius/shadow with drift (`.predictor-info-popup` had a literal `8px` radius + hardcoded `rgba(0,0,0,.08)` shadow; `.chart-info-panel` used `--color-hover`). Added `--popover-bg / --popover-border-color / --popover-radius / --popover-shadow` to `variables.css`; `.app-tooltip`, `.player-context-menu`, `.predictor-info-popup` (reused by `.luck-info-popup`), `.chart-info-panel`, and `.whatif-info-popup` (keeps its intentional dashed-accent border) all reference them — one source, no duplication. **Verified** via MCP on league.html (D table) light + x22: the player-name hover shows the themed popup (light surface / dark surface per theme), native `title` consumed, theming correct. | ◯ pending (popover tokens applied to `_theme.css` + `ChartTooltip`) | **Tokens ported:** `--popover-*` added to `src/themes/_theme.css` (derive from themed `--color-surface`/`--color-border` + global `--radius-sm`/`--shadow-md`, so all themes inherit). `src/components/ChartTooltip/chartTooltip.css` already re-pointed to them. **Still to map:** the global tooltip is the v1 form of the planned **`Tooltip` primitive** (`src/primitives/Tooltip/`, Phase 2) — port `tooltip.js` as its controller (document-level `[title]`/`[data-tooltip]` interceptor, fixed-position, a11y) and style it with `--popover-*`. The **context menu** (`PlayerCell`/playerNameInteraction) and **info popups** (`predictor/whatif/luck` in the dashboard page, Phase 6) must consume `--popover-*` too — do not re-declare the surface. Keep the whatif dashed-accent as a variant. The `.floating-btn-tooltip` removal means `AdminButton`/`ThemePicker` (Phase 3) should use the `Tooltip` primitive via `title`, not a bespoke span. |
| 2026-06-16 | (staged) | **Theme-token hardening — phantom tokens, warning split, predictor tier scale, pill unification.** Files: `css/variables.css`, `css/components.css`, `css/admin.css`, `css/index-dashboard.css`, `css/dashboard.css`, `index.html`, `js/render/landingPage.js`, `js/render/dashboardPage.js`, `js/admin/playerManager.js`. **(A) Phantom semantic tokens defined** — `--color-danger`, `--color-success` were referenced app-wide with hardcoded `var(--x, #hex)` fallbacks but **never defined anywhere**, so they ignored the theme (always the fallback). Aliased in `variables.css :root`: `--color-danger: var(--color-loss)`, `--color-success: var(--color-win)`. Fixes the luck bell-curve halves (`index-dashboard.css:923/924`), `.whatif-err`/`.whatif-del` (`dashboard.css`), `csvValidation.js`, **and the no-fallback predictor/whatif bars** (`dashboard.css:517/875` + `dashboardPage.js` — `var(--color-success)` with no fallback was resolving to nothing). **(B) `--color-warning` split by role** — one token can't be both a foreground and a background. Foreground constant `--color-warning: #d97706` (the `.whatif-warn` ⚠ icon + CSV-validation text — a caution signal intentionally theme-CONSTANT) + derived **theme-aware** background `--color-warning-bg: color-mix(in srgb, var(--color-accent-light) 60%, var(--color-warning) 40%)`. The admin pending-match block (`.match-block-pending td` at `admin.css:1119` + the `td:first-child` rule at `admin.css:1330`) now uses `var(--color-warning-bg)` instead of an inline color-mix. (Using the bg blend as a foreground was rejected: in light themes accent-light is `#dbeafe`, so the blend ≈ `#dabc9b` light tan — unreadable as text.) **(C) Probability tier scale** — NEW `--tier-high/mid/low` for the Championship-Predictor + What-If bars; `dashboardPage.js:685/1085` switched from `--color-success/warning/text-muted` to `--tier-high/mid/low`. Tints (`color-mix(... X%, transparent)`) so the % label (theme text) stays readable on the fill in **every** theme; `--tier-mid` is per-theme amber (`#b8860b` light / `#f5d76e` dark+vegas+x22 / `#ffd700` casino), high/low derive from `--color-win`/`--color-text-muted`. **Gotcha:** Chromium does **not** apply `color-mix()` inside a gradient color-stop (bars rendered solid), but it works fine as a **token value** — so the tint lives in the token and `dashboard.css` keeps a plain `var(--bar-color)` gradient. **(D) Pill unification** — `.status-pill` + `.league-type-pill` unified to one fluid size (em padding `0.2em 0.85em` + `--fs-085` + `line-height:1.5`; type-pill only adds uppercase) in `components.css`, **and the duplicate defs in `admin.css` aligned** (admin.css loads after components.css, so its stale copies were silently winning). `index.html` was the **only page missing `typography-tokens.css`**, so `--fs-*` were undefined and `.status-pill` fell back to the inherited 15px — added the link. The achievement league-type tabs (`.achv-tab`) now render as `.league-type-pill type-*` (`landingPage.js` × 4 generators; `index-dashboard.css` active = full type colour / inactive = faded), which also **removed the phantom `--color-primary` and `--color-bg-alt`** from that path. **(E) admin-msg-info** — the "player has not played any matches yet" message (`playerManager.js`) switched from a hardcoded inline `--color-bg-muted` fallback to the themed `.admin-msg admin-msg-info` classes. **Verified** via MCP across default/vegas/rainbow/casino: pills equal size + theme-following; predictor bars readable in light + dark (the original light-text-on-light-fill bug is gone); bell-curve + pending-block follow the theme. | ◯ pending (tokens applied to `_theme.css`; consumers land in later phases) | **Tokens already ported:** `--color-danger/success/warning/--color-warning-bg` added to `src/themes/_theme.css` default layer (they derive from already-themed `--color-loss`/`--color-win`/`--color-accent-light`, so all theme files inherit them — no per-theme edits). **Still to map when consumers are built:** (a) **`--tier-*` predictor scale** → fold into the **`ColorScale` component** (`src/components/ColorScale/`, which already owns red→amber→green) or add `--tier-{high,mid,low}` to `_theme.css` + per-theme `--tier-mid` in `dark/vegas/x22/casino.css` — keep the tint-as-token-value rule (color-mix in a gradient stop is a no-op in Chromium). Predictor bars land in the dashboard page phase (6). (b) **Pill sizing** — v2 already unifies both v1 pills into the **`Pill` primitive** (`.pill--{sm,md,lg}` + `.pill--uppercase`, [CHANGELOG §Pill](CHANGELOG.md)); the v1 size-unification is the interim — v2's Pill is the end state. (c) **achv-tab as type pill** → the landing achievement tabs map to **`FilterPill`/`TypePill`** in `src/pages/landing/` (Phase 6); active = full type colour, inactive = faded. (d) **admin pending block** `--color-warning-bg` → `src/pages/admin/roundEditor.css` (Phase 8). (e) `index.html` missing-typography-tokens is a v1-only packaging bug — v2 (Vite) imports tokens once globally, so it can't recur. |
| 2026-06-12 | (staged) | **Player-general C1/C2 — clickable link cells now read like the OPPONENT cell.** Files: `css/components.css` (the `td.player-cell` rule group now also targets `td.league-cell` — `font-weight: 600` on the cell + quiet `<a>` color/no-underline/hover-accent), `js/presets/playerAllMatchesPreset.js` (C2 League column gains `tdClass: 'league-cell'`; C1 in `playerLeaguesPreset.js` already had it), `table-lab/lab-loader.js` (`buildC1`/`buildC2` League columns gain `tdClass: 'league-cell'` so the design catalog stays faithful), `docs/TABLE-DESIGN.md` (new §"Clickable link cells in C1/C2"), `docs/plans/table-lab-unification.md` (Group-A list + §1.2 checklist). **Why:** the League links on the Leagues (C1) and Match History (C2) tables rendered at regular weight (400) while the OPPONENT link rendered semibold (600, from `td.player-cell`), so the two clickable columns looked inconsistent. Now both clickable columns share one rule group and look identical. **Verified** via MCP on June 2026 (`?player=Moriarty`): C1 + C2 League links and the C2 Opponent link all compute `font-weight:600`, `color: var(--color-text)`, `text-decoration:none`, accent-on-hover. | ◯ pending in v2 Phase 6 (playerGeneral) + Phase 5 (lab C1/C2 presets) | Make the C1/C2 League column a `--strong` link (see the Phase 2 `td.league-cell a` row above): the league `<a>` carries the OPPONENT look (`color: text` + `--fw-subheading` + hover-accent) via `Link.--strong`, or the cell carries `--fw-subheading` with a `--quiet` link. Do not ship the league link at regular weight. The lab presets `src/tables/presets/{C1,C2}_*.js` must set the matching cell class so the catalog matches production. |
| 2026-06-10 | (staged) | **What If — persist the "Show" (Top X) selection across re-runs + custom mobile-friendly player combobox.** File: `js/render/dashboardPage.js` (+ `css/dashboard.css`). **(1) Show persistence:** the `whatif-topx-input` select used to reset to `1` ("1st place only") on every **Run Simulation**; now a function-scope `lastTopX` remembers the last choice and each run restores it (`currentX = Math.min(lastTopX, result.n)`, clamped to the new player count). Also switched the select's `change` wiring from `addEventListener` to `onchange` so repeated runs no longer stack stale listeners. **(2) Smart player input on mobile:** replaced the two native `<datalist>` autocompletes (which behave poorly/inconsistently on mobile — often no dropdown on focus) with a custom combobox (`attachCombo(input, dropdown, getOptions)`): focus/click shows **all** options, each keystroke narrows the list (case-insensitive `includes`), full keyboard nav (Arrow/Enter/Escape), `mousedown`-based selection (fires before blur), ARIA `role=combobox/listbox/option` + `aria-expanded`. Player B's option source still narrows to A's scheduled opponents. New CSS: `.whatif-combo` (relative wrapper) + `.whatif-options`/`.whatif-option` dropdown. **Verified** via MCP on June 2026: click→25 options, type "a"→18; Run→Show=1, change to 3, Re-run→stays 3. | ◯ pending in v2 (predictor/dashboard phase) | When v2 builds the What If panel, keep both behaviours: (a) the Top-X "Show" selection must survive re-runs (store outside the run handler, clamp to player count); (b) do **not** use native `<datalist>` for the player pickers — port the custom `attachCombo` combobox (or a `Combobox` primitive) so mobile gets click-to-open-all + type-to-narrow. The combobox is page/primitive-level, not predictor-engine logic. |
| 2026-06-09 | (staged) | **League Dashboard — Progressive Disclosure (reuses the HOME tab system).** Files: `dashboard.html` (loads `css/tabs.css`), `js/render/dashboardPage.js` (the flat `renderShell()` is replaced by `mountAppTabs(...)` + four panel builders — `standingsPanel` / `matchesPanel` / `predictorPanel` / `insightsPanel`; summary cards (`#dash-cards`) stay **outside** the tabs, directly under the V16 hero, mirroring HOME's hero→info-cards→tabs), `css/dashboard.css` (`.dash-tabs-shell` + `.dash-tab-panel > .dash-section` surface-card framing, same look as HOME). **Header unchanged** — the V16 hero (type pill / Running / month) + prev-next league arrows stay exactly as-is. **Tab layout (L→R, Standings default-open):** **Standings** = Historical-view controls + table (inner `<h2>Historical view</h2>` **removed** — the tab label is enough) followed by the **Prizes & Medals** collapsible (moved here, below the table, closed by default); **Matches** = Rounds + Remaining Matches (Remaining keeps its own 3 closed sub-tabs); **Predictor** = Championship Predictor + What If; **Player insights** = the charts. **Predictor now renders for finished leagues too** — the `Running !== true` early-returns were removed from `renderPredictor` + `renderWhatIfSimulator`; for a completed season the predictor shows the "Season complete — X wins" line and What If still allows rewriting played results. **Predictor defaults:** the `?` info popup stays closed (opens on click); the What If section is renamed `What If Simulator`→**What If** and its body is **expanded by default** (arrow ▾). All render fns kept their `(ctx)` signatures + element IDs — only their mount container changed (panel vs `#content`). **Verified** via MCP: running league (June 2026) → 4 framed tabs, cards outside tabs, no "Historical view" h2, Prizes in Standings, What If header "🧪 What If" expanded, info popup open; finished league (May 2026) → Predictor tab shows "Season complete — YKwin wins" + What If present; only pre-existing optional-JSON 404s in console. | ◯ pending in v2 Phase 6 (dashboard page) | Reuse the **`SegmentedTabs` primitive** (Phase 2; see the HOME row below) for the dashboard's 4 tabs — same `urlKey:'tab'` contract, `shellClass`/`panelClass` for page framing. Keep summary cards outside the tab host. Port the panel split (Standings/Matches/Predictor/Player insights), the removed-`Historical view`-heading, Prizes-in-Standings placement, the finished-league predictor path, and the Predictor-tab defaults (info popup closed, What If expanded + renamed). The `.dash-tab-panel > .dash-section` framing is page CSS (`src/pages/dashboard/dashboard.css`), not the primitive. |
| 2026-06-09 | (staged) | **HOME tabs — extracted to a reusable controller + ARIA hardening + dead-code removal + 4-tab framing parity.** Files: NEW `js/render/appTabs.js` (`mountAppTabs({tabs, urlKey, ariaLabel, shellClass, panelClass})` — the generic tab behaviour lifted out of `landingPage.js`'s `buildTabsShell`: click activation, `?<urlKey>=` URL state with the first tab omitted as default, **full WAI-ARIA keyboard nav (roving tabindex + Arrow/Home/End)**, optional 1-N number hotkeys; returns `{root, panels, activate}`), `js/render/landingPage.js` (`buildTabsShell` reduced to a thin wrapper calling `mountAppTabs` with `shellClass:'lp-tabs-shell'` + `panelClass:'lp-tab-panel'`; **removed dead `renderNotableFigures` helper** — superseded by `renderPlayersTab`, never called), `css/tabs.css` (contract comment updated: now documents the `appTabs.js` pairing + the role=tabpanel / aria-controls / aria-labelledby a11y contract; panels emit `app-tab-panel` + caller class), `css/index-dashboard.css` (**removed the dead `.notable-*` / `.collapse-arrow` block** (~70 lines) + the mobile notable overrides; **tab-panel section framing now covers all four tabs** — selector changed from `.dash-section, .notable-figures-section` to `.dash-section, .leaderboard-section, .lp-players-section`, so **Leaderboard + Players are now framed as surface cards** like Leagues/Records; Leaderboard's flex header row (`.leaderboard-header-row`) carries the full-width divider and the legacy 2px h2 underline is dropped inside the card), `js/render/typoEditor.js` (dropped `.notable-header` from the section-heading selector + removed the 3 dead `notable-*` typo entries). **Why framing was broken:** the tabs rework only framed `.dash-section`/`.notable-figures-section`; `renderLeaderboards` emits `.leaderboard-section` and `renderPlayersTab` emits `.lp-players-section`, both of which fell through → no card. `.notable-figures-section` in the selector was itself dead (only the removed helper produced it). **Verified** via MCP on `/index.html`: all 4 panels framed (radius 10px, surface bg); ARIA cross-links (aria-controls ⇄ aria-labelledby) correct; roving tabindex + Arrow/Home/End + 1-N hotkeys + `?tab=` round-trip all working; no new console errors. | ◯ pending in v2 Phases 2 + 6 | **Promote to a primitive, not a per-page helper.** `mountAppTabs` maps to a **`SegmentedTabs` primitive (Phase 2)** — `src/primitives/SegmentedTabs/` — consumed by the landing page **and** league + playerGeneral pages in **Phase 6** (this supersedes the 2026-06-07 row's "Phase 6 landing only" note: the controller is cross-cutting). **Acceptance criteria for the v2 primitive** (now part of the public contract, hardened here in v1): (a) URL state `?<urlKey>=`, first tab omitted as default; (b) full WAI-ARIA tabs pattern — `role=tablist/tab/tabpanel`, `aria-controls` ⇄ `aria-labelledby`, roving tabindex, Arrow/Home/End; (c) panels carry a shared `app-tab-panel`-equivalent hidden contract + an optional page-framing class. Do **not** port `renderNotableFigures` — it is deleted. The 4-tab card framing is page CSS (`src/pages/landing/landing.css`), not the primitive. |
| 2026-05-28 | (staged) | `table-lab/formats/mf/mf.css` — removed `font-weight: 900` from `tr.avg-row td` (the row keeps `font-weight: 700` from the `tr.avg-row` rule). `<b>AVERAGES</b>` wrapper removed from `table-lab/lab-loader.js` (3 sites) and from `js/presets/{leagueTablePreset,playerMatchHistoryPreset}.js` (3 sites). | ◯ pending in Phase 5 | AVERAGES row was rendering at the highest possible weight (900 on cells + `<b>` label) which over-emphasised the summary row. v2 already forbids both: `900` is outside the 4-weight token scale (max 700 = `--fw-heading`), and `<b>` for visual styling is grep-gated ([PLAN.md:486](PLAN.md#L486)). When porting to `src/tables/MFTable/mfTable.css`, the row weight should map to `var(--fw-heading)`; presets must emit the plain string `'AVERAGES'`, never `<b>...</b>`. |
| 2026-05-29 | (staged) | New shared helper `js/utils/exportTableImage.js` consolidates all 5 v1 Export Image flows (D, A2, B6a, B6b, B6c). Replaces ~300 lines duplicated across `js/render/{leaguePage,landingPage,dashboardPage}.js`. Each page now exports via a 1-line wrapper that calls `exportTableImage({sourceTable, filename, title?, subtitle?, headerNode?, maxRows?})`. The helper bakes in: clone live `<table>`; `.mf-wrap` wrap for rank-* / surface-bg / divider rules; `box-shadow:none` on cells (html2canvas mis-renders inset hairlines as full-cell overlays — same root cause as the BMAB pill bug); replace BMAB pill `inset 0 0 0 1.5px currentColor` with a real border; strip `table-row-hidden`; sticky neutralisation; deterministic width via `display:inline-block` + `width:auto` + pinned `font-size`/`--space-md` (resolved at fluid-type design-max); iPhone 17 cap (`PHONE_MAX_WIDTH = 932`) with proportional shrink loop. Theme-aware bg/color/font. | ◐ ExportTableImage component scaffolded Phase 3, needs feature-parity uplift in Phase 6 | The v2 component at `src/components/ExportTableImage/exportTableImage.js` already has the close API (`{source, title, subtitle, filename}`). Phase 6 should: (a) add `headerNode` + `maxRows` params; (b) port every behaviour in the v1 helper. Best path: copy `js/utils/exportTableImage.js` verbatim into the v2 file, swap `appendExportCredit` import to the v2 path, and update the v2 ExportTableImage barrel exports. Then Phase 6 page ports become 1-line calls. |
| 2026-06-05 | (staged) | **Championship predictor — simulation model overhaul.** Files: `js/compute/championshipPredictor.js`, `js/compute/crossLeague.js`, `js/render/dashboardPage.js` (predictor + what-if call-sites, info popup, Last-300 source). Behaviour changes: **(0)** New `batchLast300PRForSimulator(playerNames)` in `crossLeague.js` pools **all non-REGULAR leagues** (doubling + ubc) into one Last-300 window (weight 7), returning `{mean, std}`; the dashboard predictor now uses this regardless of the league's own type (shared core `computeLast300Map` extracted; the old per-type `batchLast300PR` stays for site-wide display). **(1)** Always Monte Carlo — exact enumeration / Gray-code path removed; no more ≤20/>20 branching. Each remaining match, **every iteration**, draws both players' PR fresh from N(Last-300 mean, std) (Marsaglia-polar `gaussian()`), then looks up the win prob via `getWinProbability` — now **linearly interpolated** between adjacent PR-diff rows (was `Math.round`), benefiting `luckPercentile.js` too. **(2)** REGULAR uses the same combined Last-300 PR. **(3)** REGULAR Win-Rate ties resolved by a progressively-narrowing cascade over the still-tied subgroup: (a) head-to-head wins, (b) internal points-diff, (c) total points-diff vs league, (d) alphabetical — implemented via `rankRegular`/`resolveRegularTie` + base pairwise tables built from played-match scores passed in as new `playedMatches` param; a synthesized match scores winner = MatchLength, loser = ⌈ML/2⌉ (`winMargin`). UBC PR point now goes to the lower **drawn** PR (removed `prWinProbability`/normal-CDF PR-point path). Removed dead `simulateExact`/`findChampion`. **PR-based leagues (Doubling/UBC) secondary tiebreak is now a per-run Mean PR**: each iteration accumulates the same drawn PRs into `prSum` (seeded from `playedPRSum = leagueMeanPR·gamesPlayed`) and divides by `finalGames` — replacing the old static `tiebreakerPR` blend (which now serves only the X=0 season-complete path, where it collapses to the real recorded Mean PR). **Sampler:** production default is an inverse-CDF LUT (`gaussianLUT`, 4096-cell probit table via Acklam, ~32KB, lazy-built, RAM-only) + interpolated table lookup — chosen after a 12-run/1M-sim A/B on real June data showed identical rankings (maxΔ ≤ 0.06 pts, below MC noise) at ~30% less wall-time vs polar. `simOpts = { iterationsOverride, useLUT, useInterp }` keeps polar + round-lookup reachable for A/B; defaults `useLUT:true, useInterp:true`. **Iteration floor:** `estimateIterations` STEP lowered 200k→50k (both the rounding granularity and the floor) — large leagues (where the 500ms budget buys < STEP) pin to 50k. Measured on JUNE (25 players, ~220 remaining): ~0.8s @ MoE ±0.42% (was ~3.6s @ ±0.21% at the 200k floor); rankings stayed stable across 50k/100k/200k. Small leagues stay time-bound above the floor (unaffected). | ◯ pending in v2 (predictor phase) | Port `championshipPredictor.js` and the `crossLeague` Last-300 helpers verbatim. Key invariants for v2: predictor PR source must pool all non-regular leagues (not the dashboard league's type); engine is Monte-Carlo-only with per-match per-iteration PR draws feeding the interpolated table; REGULAR needs the a→b→c→alphabetical cascade and must receive played matches (with scores) to seed the pairwise tables. Numeric regression check available: the 4-player tie scenario in this session resolves P1>P2>P3>P4 deterministically. |
| 2026-06-05 | (staged) | **League-table REGULAR tiebreak — full cascade (matches the simulator).** File: `js/compute/rankings.js`. The actual standings table now resolves Win-Rate ties with the same cascade as the predictor: `buildH2H` replaced by `buildRegularTables` (pairWins + pairDiff + totalDiff from real `scoreA/scoreB`) and a recursive `resolveRegularTie` implementing **(a)** head-to-head wins → **(b)** internal points-diff → **(c)** total points-diff vs league → **alphabetical**, narrowing over the still-tied subgroup. Tie groups are now keyed on **WinRate only** (previously required equal WinRate *and* wins, and only did criterion (a) + alphabetical). PR leagues (Doubling/UBC) are unchanged — they already tiebreak on the real recorded Mean PR via the secondary sort. | ● complete | Mirrored verbatim into `v2/src/compute/rankings.js`; `v2/tests/unit/rankings.test.js` extended with a 4-way-tie cascade test (8 tests pass). The pre-existing 2-player H2H test still passes (criterion (a) decides it). |
| 2026-06-06 | (staged) | **Admin: Pending-Changes consolidation + CSV import compatibility/preview overhaul + F5 table.** Files: `js/admin/stagingStore.js`, `js/admin/render/adminPage.js`, `js/admin/leagueManager.js`, `js/admin/playerManager.js`, `js/admin/csvEditor.js`, `js/admin/roundEditor.js`, `js/admin/overridesList.js`, `js/admin/excelImporter.js`, NEW `js/admin/csvValidation.js`, `admin.html`, `table-lab/{lab.js,lab-loader.js,index.html}`. **(1) Pending-Changes:** single staged change per logical action via shared `group` (create-league, edit-players, create/edit-player fold flag+photo into the metadata group); a category-driven label formatter (`CATEGORY_META` + `renderLabel` in `adminPage.js`) gives every row `{icon}  <b>{subject}</b> · {action}{ — detail}` — drive labels off a semantic `category` field on each `addChange`, NOT off the file path (so the future DB write target swaps cleanly). **(2) Manual overrides are delta-staged:** each staged `manual_overrides.json` carries `baselineOverrides` (the published set); `diffOverrides` returns `{added, changed, removed}` and the UI/badge show only the delta (⚖️ added / ✏️ edited / ➖ removed), each individually cancellable (`removeOverrideFromChange` for added, `restoreOverrideToChange` reverts edited/removed to the published value). All 4 override write-sites route through `stageManualOverrides`. **(3) CSV import compatibility report** (`csvValidation.js`): player-count/roster diff, Levenshtein typo suspects, fewer-matches regression warning, and override-collision warning (CSV results shadowed by a manual override won't apply — overrides win via `applyOverrides`). Rendered directly under the drop zone, never blocks. **(4) F5 — CSV Import Preview** table: read-only **MF** (`mountMFTable`, `font-small`, `stickyCols:1`) showing only the "N updates" (played-in-upload, not already played, not override-covered) from `report.newMatches`; `admin.html` loads `table-lab/formats/mf/mf.css`; catalogued in the lab + `docs/TABLE-DESIGN.md` (F5) + `docs/plans/table-lab-unification.md`. | ◯ pending in v2 (Phase 8 admin + Phase 5 lab F5) | Port `csvValidation.js` + the `excelImporter.js` preview wiring + the `stagingStore.js` group/category/override-delta model. v2 admin is read-only until Phase 8, so this lands then; the F5 lab preset lands in Phase 5 alongside the other presets (`buildF5`). Keep the category-based label formatter (path-agnostic) — it is the seam for the eventual DB-backed write target. |
| 2026-06-06 | (staged) | **F6 — Medals & Prizes table moved onto the FF format (Edit League + Add New League).** Files: `js/admin/leagueManager.js` (new shared `ffMedalsTableHTML` builder; both Edit and Add call it), `css/admin.css` (bespoke `.medal-prize-table` chrome removed → replaced by the shared FF chrome `.ff-wrap` + `.admin-table.font-large` with `data-mf-table-id="F6"`; only F6-specific bits kept: equal Count/Prize column widths `[data-mf-table-id="F6"] *:nth-child(n+2){width:28%}` + `.medal-cell` inline-flex/colour), `docs/TABLE-DESIGN.md` (F6 added to the F-table list + FF usage + §FF note), `docs/plans/table-lab-unification.md` (F6 added to the Phase-8 FF scope + 8.1 inventory), `js/render/typoEditor.js` (`f6-table` entry targeting `[data-mf-table-id="F6"]`). Display medal cell (icon + colored name) + two Edit cells (Count, Prize number inputs keep stable ids so the Save/Create handlers read them unchanged). **Font:** FF chrome is keyed to `.admin-table.font-large`, so F6 is **font-large** (was bespoke font-small); the Count/Prize inputs keep responsive font-small via `.edit-card-sm .form-group input`. **Lab not yet wired:** like F1–F4, F6 is hand-built FF chrome — `mountFFTable` rewiring is deferred to Phase 8 (the table-lab integration was intentionally NOT done now). | ◯ pending in v2 (Phase 8 admin + Phase 5 lab FF) | When v2 builds the admin Edit/Add League pages (Phase 8) and the FormTable lab catalog (Phase 5), render F6 through `FormTable`/`mountFFTable` with a Medals preset (Display medal col + Count/Prize Edit cols). Decide then whether to add a font-small FF chrome variant so the medals table can be font-small in v2; otherwise keep it font-large like the other FF tables. Shares the FF format with F1/F2/F2b/F4. |
| 2026-06-06 | (staged) | **Favicon — league logo as browser-tab icon on every page.** New tracked asset `assets/favicon.png` (copy of `assets/logo/logo.png`; placed under `assets/` because the repo-root `/*.png` .gitignore rule silently drops any `favicon.png` at root). `<link rel="icon" type="image/png" href="assets/favicon.png">` added to all v1 pages: `index.html`, `league.html`, `player.html`, `admin.html`, `dashboard.html`, `player_general.html`, `typo-editor.html`, `design-lab.html`, `design-catalogue.html`, and `table-lab/index.html` (relative `../assets/favicon.png`). **Also: link-preview meta** (Open Graph `og:*` + Twitter `summary` card) pointing `og:image`/`twitter:image` at the **absolute** URL `https://golan.me.uk/assets/favicon.png` (640×640), plus `<meta name="description">`, added to the 5 public pages only — `index/league/player/dashboard/player_general` (skipped admin + dev tools intentionally). | ◯ pending across Phases 5–8 | v2 is a Vite app: drop the logo at `v2/public/favicon.png` (Vite serves `public/` at the site root) and add `<link rel="icon" type="image/png" href="/favicon.png">` to each page `<head>` as it is built — landing/league/dashboard/player/playerGeneral (Phase 6), tableLab (Phase 5), designLab/typoEditor (Phase 7), admin (Phase 8), catalogue (Phase 1+). No per-page relative paths needed since `/favicon.png` resolves from root. For the **link-preview** tags: `og:image`/`twitter:image` MUST stay an absolute URL (`https://golan.me.uk/...`) — relative paths are not resolved by social scrapers; replicate the OG + Twitter `summary` block on the 5 public pages only. Watch the `/*.png` root .gitignore does not also exist in `v2/` — if it does, `v2/public/` is exempt anyway. |
| 2026-06-08 | (staged) | **HOME — Players tab + A7 (new SF preset).** Files: `js/render/landingPage.js` (new `renderPlayersTab` + `computePlayerRows` + `sortPlayerRows` + `buildPlayerCols`; orchestrator routes `_playersMeta + leagues` into the Players tab instead of `renderNotableFigures`; `buildTabsShell` now declares 4 tabs with no count badges; the rightmost tab is `players`; the legacy `renderNotableFigures` helper was **removed** in the 2026-06-09 cleanup below — it was superseded by `renderPlayersTab` and never called), `css/index-dashboard.css` (new `.lp-players-section` spacing + `.lp-status` / `.lp-status-dot` / `.lp-realname` / `.lp-muted` + theme overrides; the previous `.lp-subtabs*` styles removed — UI is two stacked SF tables instead of nested tabs), `index.html` (now also loads `table-lab/formats/sf/sf.css`), `docs/TABLE-DESIGN.md` (A7 entry under §A + new A7 sub-section in SF), `docs/plans/table-lab-unification.md` (A7 added to SF scope across Phases 5 + 7). **Table layout:** A7 is two SF instances stacked vertically (Notable Figures + Rest of Players), each with its own `title` (count in the title), `tableId: 'A7'`, `fontClass: 'font-small'`, `stickyCols: 1`. **Cols:** Player (flag + link + real-name) / Status (active|inactive pill, `.lp-status`) / Last Active (`<a class="league-link">` matching A6) / Title (italic title description). **Sort:** fixed — status (active first) → alphabetical case-insensitive. **showTopN:** only on the Rest instance (15). **Bug fix while building:** `l.allPlayers` is a `Set`, not an array, so `.includes()` → `.has()` in `lastLeagueFor()`. | ◯ pending in v2 Phases 3+5+6 | **v2 mapping:** A7 lives in `src/tables/presets/A7_playersDirectory.js` (new). The two-table-stack layout maps to a small `PlayersSection` page-level component in `src/pages/landing/players.js` that calls `mountSFTable` twice and shares one `cols` array. The new `.lp-status` pill becomes `StatusChip --player` variant in `src/components/StatusChip/`. The `.lp-realname` "hidden on mobile" rule belongs in the `PlayerCell` component (already in Phase 3 plan). `playersMetadata.json` `hidden` flag must continue to filter rows. |
| 2026-06-07 | (staged) | **HOME — Progressive Disclosure: hero CTA + 4 segmented tabs.** Files: `js/render/landingPage.js` (orchestrator rewire — `renderLandingPage` now appends a single big `<a class="lp-hero-cta">` when exactly one league is `Running`, then routes the 9 existing render fns into 4 tab panels via a new `buildTabsShell({running, completed, people, recordsCount})` helper), `css/index-dashboard.css` (new section at end: `.lp-hero-cta`, `.lp-tabs-shell`, `.lp-tabs` (sticky + backdrop-blur), `.lp-tab`, `.lp-tab-count` badge, `.lp-tab-panel[hidden]`, mobile overrides). Tabs: **Leagues** (Info Cards + Active + Completed) / **Leaderboard** (A2 annual) / **People** (Notable Figures) / **Records** (A3+A4+A5+A6). Default tab = Leagues. URL state via `?tab=<id>` (replaceState, so back-button works; `leagues` is omitted from URL as it's the default). Tab counts displayed in pill-badges (e.g. `Leagues 10`, `People 6`, `Records 4`). Hotkeys 1-4 jump tabs (only when focus is not in an input). **No existing render function was modified** — they still take `(container, …)` and append a `<section>`; the orchestrator just gives them a tab-panel instead of the page container. The pre-existing `.collapsible-section` pattern on Completed Leagues is preserved. | ◯ pending in v2 Phase 6 (landing page) | **v2 port**: the `buildTabsShell` helper maps cleanly to a `SegmentedTabs` primitive + a `TabPanels` host in `src/components/Navigation/` (or `src/primitives/SegmentedTabs/`). The hero CTA is a `Button --primary --hero` variant. Reuse this exact tab grouping for v2's `src/pages/landing/landing.js`. **Acceptance check**: visit `/index.html?tab=records` in v1 and `/landing.html?tab=records` in v2 → both should land on Records tab with identical sections rendered. URL state contract is part of the public API. |
| 2026-06-07 | `09e91af` + `4105779` | **Multi-league BGStudio sync — CI backend + admin sync card + checkbox/radio CSS reset.** Files: `scripts/sync-bgstudio.js`, `.github/workflows/sync-bgstudio.yml`, `js/admin/leagueManager.js` (BGStudio Sync card on Edit-League), `css/admin.css` (scoped reset so `.form-group input[type="checkbox"]`/`[type="radio"]`, including under `.edit-card-sm`, don't inherit the full-width+padded sizing from the generic `.form-group input` rule — fixes the previously-broken checkbox layout that pushed labels off-row), `leagues/.../leaguedata.csv` (CI-refreshed data), `docs/plans/supabase-migration.md`. **(1) CI backend:** parameterized `exportLeagueTask(bgstudioLeagueName, folder)`, `LEAGUES` env-var parsing, `runAllExports()` loop driven by parsed targets; output paths moved from `out/*` to `out/<folder>/*` so N leagues' artifacts coexist. Workflow `workflow_dispatch.inputs.leagues` JSON-string input + LEAGUES env. **(2) Sync timing:** manual Run defaults to `fast` (~20s); scheduled / external dispatches forced to `full` via `github.event_name == 'workflow_dispatch'` guard. `navigateToLeaguesList` collapsed to a single `s(113,'0')` direct nav with polling + waitFor on the leagues-list button (removes the Tournaments-tab + Leagues-row click sequence that silently failed in fast mode). **(3) "Sync together with" UI — REVERTED.** The Edit-League → BGStudio Sync `params.BGStudioSync.syncWith` (array of folder names) UI from `09e91af` was reverted by `4105779` because the workflow now takes a LEAGUES JSON input directly, so per-league pairing UI is unnecessary. No `league_params.json` schema change net of the two commits. | ◯ pending in v2 Phase 8 (admin) + Phase 2 (FormField) | v2 mapping: (a) Port the BGStudio Sync card (form-group inputs + "Save BGStudio Settings" handler reading from / writing to `params.BGStudioSync`) into `src/pages/admin/leagueManager.js` Edit-League view. (b) Port the **checkbox/radio scoped reset** into `src/primitives/FormField/formField.css` — it is a genuine bug fix (without it, custom checkboxes inherit `width:100%` + input padding from the form-group rule and the label flows to the next line). (c) Do **NOT** port the "Sync together with" UI — it was reverted. (d) The CI infrastructure (`scripts/sync-bgstudio.js`, `.github/workflows/sync-bgstudio.yml`) lives outside both v1 and v2 — it ingests into `leagues/` which is shared. No v2 mapping needed for the script/workflow themselves. |
| 2026-06-04 | (staged) | **Path-X canonization sweep — units policy + 5 px→em violations.** New `Units policy` doc-block at top of `table-lab/formats/base/base.css` (the load-bearing rule: em for sizing-with-font, px for hairlines/shadows/viewport-caps/breakpoints/JS-fallbacks). Canonical `.flag` and `.show-more-btn` rules added to `base.css` and mirrored into `css/components.css` + `css/admin.css` (admin doesn't load base). 5 violation classes eliminated: (1) flag size/margin/border-radius converted to em across base+mf+sf+exp+ff canon AND production mirrors `dashboard.css`, `index-dashboard.css`, `player-general.css`; (2) `.show-more-btn` consolidated to base.css em-based — duplicates removed from mf+sf+index-dashboard+player-general+dashboard.css; (3) predictor/whatif pct bars em-based in mf.css + dashboard.css; (4) exp container (`.pg-rank-expanded`) em-based; (5) sort-icon, level-cell letter-spacing, mobile matrix compression padding all em. `image-rendering:crisp-edges` removed from `.pg-matches-table .flag` (was forcing nearest-neighbor scaling on small thumbnails) — all `.flag` declarations now explicit `image-rendering:auto`. Phase 7.1 wiring landed alongside: `<link rel="stylesheet" href="table-lab/formats/{sf,exp}/...css">` added to `league.html`, `player.html`, `player_general.html`, `dashboard.html`. | ◯ pending in v2 Phase 5 | v2 Phase 5 (`src/tables/{MFTable,SFTable,ExpandableTable,FormTable}/`) should source from the canonized v1 lab files directly — `table-lab/formats/{base,mf,sf,exp,ff}/*.css` are now authoritative (the legacy components.css/dashboard.css/index-dashboard.css/player-general.css copies live alongside but lose by cascade in production). The Units policy block at the top of `base.css` should port verbatim into the v2 table CSS architecture as a non-negotiable foundation rule. The canonical `.flag` rule should land in v2 as a single shared atom (likely `src/components/Flag/flag.css` or a shared table primitive). |
